# UltiTools-API Deprecation Registry

Generated by `DeprecationRegistryGenerator` (GEN-12) from the `@Deprecated` annotations and `@deprecated`/`@removeIn` javadoc tags in `src/main/java`, cross-checked against the japicmp report. Do not hand-edit - it is regenerated on every `mvn verify` and any manual change is overwritten.

## Removed

| Member | Since | Removed in | Replacement |
|---|---|---|---|
| `com.ultikits.ultitools.UltiTools#getVersionWrapper()` | 6.2.0 | 6.3.0 | Use XVersionUtils instead. |
| `com.ultikits.ultitools.UltiTools#versionWrapper` | 6.2.0 | 6.3.0 | Use XVersionUtils instead. |
| `com.ultikits.ultitools.abstracts.AbstractCommandExecutor` | 6.2.0 | 6.3.0 | This class is maintained for backward compatibility. Use BaseCommandExecutor instead, which provides better architecture with Chain of Responsibility pattern. |
| `com.ultikits.ultitools.abstracts.AbstractCommendExecutor` | 6.2.1 | 6.3.0 | Use AbstractCommandExecutor instead. |
| `com.ultikits.ultitools.abstracts.AbstractDataEntity` | 6.2.0 | 6.3.0 | This class uses Object type for ID which is not type-safe. Use BaseDataEntity instead, which provides generic ID type and lifecycle hooks. |
| `com.ultikits.ultitools.abstracts.UltiToolsPlugin#UltiToolsPlugin(java.lang.String,java.lang.String,java.util.List,java.util.List,int,java.lang.String)` | 6.0.8 | 6.3.0 | Use the seven-argument constructor and pass `resourceFolderPath` explicitly. This overload hard-codes it to `<dataFolder>/pluginConfig/<pluginName>`. |
| `com.ultikits.ultitools.abstracts.UltiToolsPlugin#getVersionWrapper()` | 6.2.0 | 6.3.0 | Use XVersionUtils instead. |
| `com.ultikits.ultitools.abstracts.guis.OkCancelPage` | 6.2.0 | 6.3.0 | Use BaseConfirmationPage instead, which provides better template method pattern, builder support, and code organization. |
| `com.ultikits.ultitools.abstracts.guis.PagingPage` | 6.2.0 | 6.3.0 | Use BasePaginationPage instead, which provides better template method pattern and code organization. |
| `com.ultikits.ultitools.annotations.command.OptionalParam` | 6.0.7 | 6.3.0 | Never implemented — no code reads this annotation, so applying it has no effect on command parsing. There is no replacement: declare a separate `@CmdMapping` format for each accepted argument shape. |
| `com.ultikits.ultitools.context.UltiToolsBean#getVersionWrapper()` | 6.2.0 | 6.3.0 | Use XVersionUtils instead. |
| `com.ultikits.ultitools.interfaces.TempListener#player(java.lang.Class)` | 6.1.0 | 6.3.0 | Use common(Class) instead. You can use DefaultTempListenerBuilder#filter(Function) to filter player events. |
| `com.ultikits.ultitools.interfaces.TempListener$PlayerTempListenerBuilder` | 6.1.0 | 6.3.0 | Use common(Class) instead, narrowing with DefaultTempListenerBuilder#filter(Function). |
| `com.ultikits.ultitools.interfaces.VersionWrapper` | 6.2.0 | 6.3.0 | Use XVersionUtils static methods instead. This interface is kept for backward compatibility only. Will be removed in a future version. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getBed(com.ultikits.ultitools.entities.Colors)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getBed(Colors) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getBlockFace(org.bukkit.block.Block)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getBlockFace(Block) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getColoredPlaneGlass(com.ultikits.ultitools.entities.Colors)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getColoredPlaneGlass(Colors) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getEmailMaterial(boolean)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getEmailMaterial(boolean) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getEndEye()` | 6.2.0 | 6.3.0 | Use XVersionUtils#getEndEye() instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getGrassBlock()` | 6.2.0 | 6.3.0 | Use XVersionUtils#getGrassBlock() instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getHead(org.bukkit.OfflinePlayer)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getHead(OfflinePlayer) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getItemDurability(org.bukkit.inventory.ItemStack)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getItemDurability(ItemStack) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getItemInHand(org.bukkit.entity.Player,boolean)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getItemInHand(Player, boolean) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getSign()` | 6.2.0 | 6.3.0 | Use XVersionUtils#getSign() instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#getSound(com.ultikits.ultitools.entities.Sounds)` | 6.2.0 | 6.3.0 | Use XVersionUtils#getSound(Sounds) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#registerNewObjective(org.bukkit.scoreboard.Scoreboard,java.lang.String,java.lang.String,java.lang.String)` | 6.2.0 | 6.3.0 | Use XVersionUtils#registerNewObjective(Scoreboard, String, String, String) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#sendActionBar(org.bukkit.entity.Player,java.lang.String)` | 6.2.0 | 6.3.0 | Use XVersionUtils#sendActionBar(Player, String) instead. |
| `com.ultikits.ultitools.interfaces.VersionWrapper#sendPlayerList(org.bukkit.entity.Player,java.lang.String,java.lang.String)` | 6.2.0 | 6.3.0 | Use XVersionUtils#sendPlayerList(Player, String, String) instead. |
| `com.ultikits.ultitools.interfaces.impl.DefaultVersionWrapper` | 6.2.0 | 6.3.0 | Use XVersionUtils directly instead. |
| `com.ultikits.ultitools.interfaces.impl.PlayerTempListener` | 6.1.0 | 6.3.0 | Use TempListener#common(Class) and filter by player instead. |
| `com.ultikits.ultitools.manager.CommandManager#register(org.bukkit.command.CommandExecutor)` | 6.0.5 | 6.3.0 | Use register(UltiToolsPlugin, Class) instead. |
| `com.ultikits.ultitools.manager.CommandManager#register(org.bukkit.command.CommandExecutor,java.lang.String,java.lang.String,java.lang.String[])` | 6.0.5 | 6.3.0 | Use register(UltiToolsPlugin, Class, String, String, String...) instead. |
| `com.ultikits.ultitools.manager.CommandManager#registerAll(com.ultikits.ultitools.abstracts.UltiToolsPlugin,java.lang.String)` | 6.2.5 | 6.3.0 | This overload casts every scanned class to AbstractCommandExecutor, which is itself scheduled for removal in 6.3.0. A command class written against the current BaseCommandExecutor does not extend that type, so the cast throws ClassCastException — and the surrounding catch lists only the four reflective checked exceptions, so it escapes. Use registerAll(UltiToolsPlugin), which resolves command classes as beans from the plugin's container and performs no cast. See issue #272. |
| `com.ultikits.ultitools.manager.ListenerManager#register(com.ultikits.ultitools.abstracts.UltiToolsPlugin,org.bukkit.event.Listener)` | 6.0.5 | 6.3.0 | Use register(UltiToolsPlugin, Class) instead; this overload takes an already-constructed instance and therefore performs no dependency injection. |
| `com.ultikits.ultitools.manager.ListenerManager#registerAll(com.ultikits.ultitools.abstracts.UltiToolsPlugin,java.lang.String)` | 6.3.0 | 6.3.0 | This overload reflectively scans and instantiates every `@EventListener` class in `packageName` without ever consulting EventListener#manualRegister() -- a listener its author deliberately withheld from auto-registration is registered anyway. After plan 04-08, PluginManager#registerBukkit no longer has an in-framework caller for this overload either; both registration entry points resolve listeners as beans from the module's own container instead. Use registerAll(UltiToolsPlugin), which does honour `manualRegister()`. See issue #337. |
| `com.ultikits.ultitools.manager.PluginManager#initializePlugin(java.lang.ClassLoader,java.lang.Class,java.lang.Object[])` | 6.3.0 | 6.3.0 | This reflective, with-args construction path has failed on every release since 6.2.0 (Phase 1 D-15, measured): SecurityPolicy#isSafeParameterType matches collection arguments by exact runtime-class-name prefix (`java.util.List`, `java.util.ArrayList`, ...), and neither `Arrays.asList(...)`'s runtime type (`java.util.Arrays$ArrayList`) nor any `Collections.*` wrapper type matches any of those prefixes -- the list-typed arguments a caller actually supplies are rejected before construction ever runs. This path is scheduled for removal (issue #332) rather than repair; only the seven-argument register(Class, String, String, List, List, int, String) calls it with a non-empty argument array. |
| `com.ultikits.ultitools.manager.PluginManager#register(java.lang.Class,java.lang.String,java.lang.String,java.util.List,java.util.List,int,java.lang.String)` | 6.3.0 | 6.3.0 | This overload's reflective, with-args construction has failed on every release since 6.2.0 (Phase 1 D-15, measured): SecurityPolicy#isSafeParameterType rejects the runtime types `authors` and `loadAfter` actually are (`Arrays.asList(...)`'s `java.util.Arrays$ArrayList`, or any `Collections.*` wrapper) before the module's constructor ever runs. This overload existed to bypass `plugin.yml`-based metadata for connector callers; use register(UltiToolsPlugin) instead, which takes an already-constructed plugin instance and has no reflective construction path of its own. See issue #332. |

## Announced for removal

| Member | Since | Scheduled removal | Replacement |
|---|---|---|---|
| `com.ultikits.ultitools.interfaces.DataStore#getOperator(com.ultikits.ultitools.abstracts.UltiToolsPlugin,java.lang.Class)` | 6.3.0 | 6.4.0 | Carries no ownership check of its own (D-14/D-18) -- a `DataStore` implementation that overrides this method directly must call checkOwnership(UltiToolsPlugin, Class) as its own first statement to get the refusal; the three backends shipped by this framework all do (02-12). Framework- internal callers (`UltiToolsPlugin.getDataOperator`) already check ownership themselves before calling this. Use getOperator(DataScope, Class). |
| `com.ultikits.ultitools.interfaces.DataStore#getOperator(java.io.File,java.lang.Class)` | 6.3.0 | 6.4.0 | Enforces D-18's ownership check via checkOwnership(java.io.File, Class) as its own first statement -- a `DataStore` implementation that overrides this method directly must call the same helper to get the refusal; the three backends shipped by this framework all do (02-12). Framework-internal callers (`UltiToolsAPI.getDataOperator`) already check ownership themselves before calling this. Use getOperator(DataScope, Class). |
| `com.ultikits.ultitools.interfaces.TransactionManager#getConnection()` | 6.3.0 | 6.4.0 | JDBC-specific; not every `TransactionManager` backs a DataSource. Program against JdbcTransactionManager#getConnection() instead. This default throws UnsupportedOperationException for any backend that only implements the base interface. |
| `com.ultikits.ultitools.interfaces.TransactionManager#setIsolationLevel(int)` | 6.3.0 | 6.4.0 | JDBC-specific; program against JdbcTransactionManager#setIsolationLevel(int) instead. This default throws UnsupportedOperationException for any backend that only implements the base interface. |
| `com.ultikits.ultitools.interfaces.TransactionManager#setReadOnly(boolean)` | 6.3.0 | 6.4.0 | JDBC-specific; program against JdbcTransactionManager#setReadOnly(boolean) instead. This default throws UnsupportedOperationException for any backend that only implements the base interface. |
| `com.ultikits.ultitools.interfaces.impl.SimpleTempListener#SimpleTempListener()` | 6.3.0 | 6.4.0 | Use the four-argument `(Class, EventPriority, TempEventHandler, Function)` constructor (Lombok-generated via `@AllArgsConstructor`, so it has no source declaration to link to) or TempListener#common(Class). Scheduled for removal in 6.4.0. |
| `com.ultikits.ultitools.interfaces.impl.SimpleTempListener#SimpleTempListener(java.lang.Class,com.ultikits.ultitools.interfaces.TempEventHandler)` | 6.3.0 | 6.4.0 | Use the four-argument `(Class, EventPriority, TempEventHandler, Function)` constructor (Lombok-generated via `@AllArgsConstructor`, so it has no source declaration to link to) or TempListener#common(Class). Scheduled for removal in 6.4.0. |
| `com.ultikits.ultitools.interfaces.impl.SimpleTempListener#SimpleTempListener(java.lang.Class,com.ultikits.ultitools.interfaces.TempEventHandler,java.util.function.Function)` | 6.3.0 | 6.4.0 | Use the four-argument `(Class, EventPriority, TempEventHandler, Function)` constructor (Lombok-generated via `@AllArgsConstructor`, so it has no source declaration to link to) or TempListener#common(Class). Scheduled for removal in 6.4.0. |
| `com.ultikits.ultitools.interfaces.impl.SimpleTempListener#SimpleTempListener(java.lang.Class,com.ultikits.ultitools.interfaces.TempEventHandler,org.bukkit.event.EventPriority)` | 6.3.0 | 6.4.0 | Use the four-argument `(Class, EventPriority, TempEventHandler, Function)` constructor (Lombok-generated via `@AllArgsConstructor`, so it has no source declaration to link to) or TempListener#common(Class). Scheduled for removal in 6.4.0. |
| `com.ultikits.ultitools.manager.PluginDependencyResolver$PluginNode#getAllDependencies(java.util.Set)` | 6.3.0 | 6.4.0 | use getAllDependencies(Set, Map), which resolves each entry through the plugin.yml-name alias map (D-12) before checking availability |
| `com.ultikits.ultitools.utils.AnnotationUtils#findAnnotation(java.lang.Class,java.lang.Class)` | 6.3.0 | 6.4.0 | This legacy lookup only walks one meta-annotation level and never merges `@AliasFor` overrides. Use MergedAnnotationResolver#find instead, which walks the whole annotation tree and applies any `@AliasFor` override found along the way. |

## Deprecated

| Member | Since | Scheduled removal | Replacement |
|---|---|---|---|
| `com.ultikits.ultitools.utils.SecurityPolicy#addDangerousClass(java.lang.String)` | 6.3.0 | - | Since 6.3.0 (GEN-07): this method no longer mutates `ClassloadFilterAudit.SYSTEM_DANGEROUS_CLASSES` or anything else. The signature is retained because it is `public static` on a published class. |
| `com.ultikits.ultitools.utils.SecurityPolicy#addTrustedPackage(java.lang.String)` | 6.3.0 | - | Since 6.3.0 (GEN-07): this method no longer mutates `ClassloadFilterAudit.TRUSTED_PACKAGE_PREFIXES` or anything else. The signature is retained because it is `public static` on a published class. |
| `com.ultikits.ultitools.utils.SecurityPolicy#isSafeClassName(java.lang.String)` | 6.3.0 | - | Since 6.3.0 (GEN-07) this method is an unconditional no-op -- the three name-based classload filter layers it enforced never protected anything (they evaluated already-trusted, already-loaded classes, not arbitrary untrusted input), and the trusted-package whitelist alone would have refused nearly every third-party module class. The signature is retained because it is `public static` on a published class and cannot be removed without breaking a downstream caller's compile. See the package-private `ClassloadFilterAudit` for what these removed layers would have refused, now recorded as telemetry. |
| `com.ultikits.ultitools.utils.SecurityPolicy#isSafeParameterType(java.lang.Class)` | 6.3.0 | - | Since 6.3.0 (GEN-07), for the same reason as isSafeClassName(String) -- its only in-tree caller (the seven-argument `PluginManager.register`/with-args reflective-construction path) was deleted by GEN-04, which is what dissolved this method's former "shared-field, can't safely delete the whitelist" objection. |

