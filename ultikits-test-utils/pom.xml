<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ultikits</groupId>
    <artifactId>ultikits-test-utils</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>UltiKits Test Utilities</name>
    <description>Shared test helpers for UltiKits plugin module development</description>
    <url>https://github.com/UltiKits/UltiTools-Reborn/tree/main/ultikits-test-utils</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://raw.githubusercontent.com/UltiKits/UltiTools-Reborn/main/ultikits-test-utils/LICENSE</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git@github.com:UltiKits/UltiTools-Reborn.git</connection>
        <developerConnection>scm:git@github.com:UltiKits/UltiTools-Reborn.git</developerConnection>
        <url>https://github.com/UltiKits/UltiTools-Reborn/tree/main/ultikits-test-utils</url>
    </scm>

    <developers>
        <developer>
            <name>wisdomme</name>
            <email>xia.erich03@gmail.com</email>
            <organization>UltiKits</organization>
            <timezone>+10</timezone>
        </developer>
    </developers>

    <properties>
        <java.version>1.8</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <distributionManagement>
        <snapshotRepository>
            <id>ultikits-sonatype</id>
            <url>https://s01.oss.sonatype.org/content/repositories/snapshots</url>
        </snapshotRepository>
        <repository>
            <id>ultikits-sonatype</id>
            <url>https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/</url>
        </repository>
    </distributionManagement>

    <dependencies>
        <!-- Provided at compile time, required by consumers at test time -->
        <dependency>
            <groupId>com.ultikits</groupId>
            <artifactId>UltiTools-API</artifactId>
            <version>6.2.5</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.20.4-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.5.0</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.10.0</version>
            <scope>compile</scope>
        </dependency>
        <!-- AssertJ for fluent test assertions -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.7</version>
            <scope>compile</scope>
        </dependency>
        <!-- JUnit engine for running our own tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>2.2.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <show>private</show>
                    <nohelp>true</nohelp>
                    <charset>UTF-8</charset>
                    <encoding>UTF-8</encoding>
                    <docencoding>UTF-8</docencoding>
                    <locale>en_US</locale>
                    <failOnWarnings>false</failOnWarnings>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>0.9.0</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>ultikits-sonatype</publishingServerId>
                    <!--
                        Staging-only by design. autoPublish=false uploads the bundle, lets Central
                        run its validation, and stops for a human to release. waitUntil must be
                        `validated`, NOT `published`: with autoPublish=false the deployment never
                        reaches PUBLISHED on its own, so `published` would block waiting for a
                        state only a manual release can produce.

                        This module is prepared but NOT publishable, and the reason is now measured
                        rather than argued. A real staging attempt on 2026-09-03 was REJECTED by
                        Central's own validator:

                          Deployment 6db570a1-29fa-4c56-bce8-f2f98ed4d498 failed
                          pkg:maven/com.ultikits/ultikits-test-utils@1.0.0:
                           - Dependencies to SNAPSHOT versions not allowed for dependency:
                             org.spigotmc:spigot-api

                        Note a genuine counter-observation that does NOT overturn this, so nobody
                        re-litigates it: com.github.cryptomorin:XSeries:13.3.2 is served by Central
                        today WITH org.spigotmc:spigot:1.21.6-R0.1-SNAPSHOT in provided scope. It
                        was published 2025-06-29, one day before legacy OSSRH shut down, and OSSRH
                        did not enforce this rule. The Central Portal does, and the Portal is the
                        only path now. Both facts are true; the publishing channel is what changed.

                        No released Bukkit-family coordinate can be substituted either: none has
                        ever existed, for licensing reasons dating to CraftBukkit's 2014 takedown
                        (see 08-GATE04-SPIKE.md). Resolving this needs one of the two architectural
                        options in 08-RELEASE-PACKAGE.md #2.2 - a maintainer decision, not a config
                        change.

                        Do not flip autoPublish to true. Release authorisation is `accept-only`.
                    -->
                    <autoPublish>false</autoPublish>
                    <waitUntil>validated</waitUntil>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>spigotmc-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <repository>
            <id>sonatype-releases</id>
            <url>https://s01.oss.sonatype.org/content/repositories/releases/</url>
        </repository>
    </repositories>

    <profiles>
        <!--
            Release-only profile: GPG signing is activated here and nowhere else.
            Only Maven Central needs signed artifacts, and the private key exists
            only in the release environment. Binding maven-gpg-plugin to verify
            unconditionally breaks mvn verify and mvn install for everyone without
            a key. Forgetting -P release fails loudly at Central's validation
            instead of silently publishing unsigned artifacts.
        -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>1.6</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
