# UltiTools-API — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Delegated-authorization UltiCloud rows (D-27b, corrected by Phase 15 D-01):** a row whose
  Steps can only be exercised through an authenticated UltiCloud panel session — because the real
  inbound WebSocket route requires a valid cloud token issued by a magic-link login — carries the
  fixed Preconditions phrase `an authorized UltiCloud session under the maintainer's delegated
  authorization` (appended to, not replacing, the row's own preconditions) and Layer `server`. As
  of Phase 15 D-01, the maintainer delegates that authorization to the dispatching agent directly
  (approving the printed magic link, scoped to the shared UAT server only), so these rows are
  executable and observed through the running server rather than ending at `human-uat-pending` by
  design; a row still blocked pending the shared server's own UltiCloud activation records that as
  its dispatch-time `blocked` status in the canonical verdict file, not as a property of this
  checklist's Layer column.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class or per
  shipped yml file, never one row per key. This is the one deliberate exception to the "ID cites
  its `FEATURES.md` ID verbatim" rule above: a config-per-file row (ID suffixed `-yml`, e.g.
  `ultitools.config.config-yml`) aggregates every per-key `ultitools.config.<file-stem>.*` row for
  that file rather than citing a single one of them — there is no single per-key ID to cite when
  the row's job is exercising the whole file at once. A build-time (Maven-filtered) property —
  one resolved by Maven resource filtering at package time, such as `env.yml`'s `api-url` — is
  exercised by the build that produced the handed-over jar, not by editing the shipped file after
  the fact; such a row must state which build property it proves, and its Preconditions must
  state that the file it reads comes from a fresh, unmodified extraction of that build's jar at
  the property's shipped default, not a hand-edited copy.
- This repository's shipped default is `language: "zh"` in `config.yml`. Every row below whose
  Expected quotes a literal in-game or console line therefore carries the precondition
  `language: en` set in `plugins/UltiTools/config.yml`, so the observed line matches this
  document's English-only text exactly, character for character.

## /ul — framework administration

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.ul.help | `language: en` in config.yml | Run `/ul help` (console or op player) | Chat/console output is exactly `=== UltiTools Commands ===` followed by `/ul reload Reload all modules`, `/ul reload <name> Reload a specific module`, `/ul list Check loaded module list`, `================` | server | |
| ultitools.ul.list | At least one KNOWN module confirmed successfully loaded (its name appears in the server's own startup log as enabled, not merely a JAR present in `plugins/UltiTools/plugins/` — an invalid module, or one excluded by `PluginDependencyResolver` for a missing or circular hard dependency, leaves `PluginManager#getPluginList()` without an entry despite the JAR being present) | Run `/ul list` | One line per loaded module reading `<module name> <version>`, matching `UltiToolsPlugin#getPluginName` and `#getVersion` for every module actually present, INCLUDING a line for the known successfully-loaded module named in the precondition — a zero-line or module-missing result is a fail, not a vacuous pass | server | |
| ultitools.ul.reload | Same precondition as ultitools.ul.list (appears after it), PLUS the UltiChat module specifically loaded and successfully enabled (the generic "at least one KNOWN module" precondition it inherits is not enough here — this row's second fixture needs `config/chat.yml` and its `chat.format`-driven behaviour to exist at all, which only UltiChat's own JAR provides); two concrete, named fixtures, not merely "every loaded module" in the abstract — a partial reload that updates only one module cannot be distinguished from a full reload without at least two independent fixtures: (1) the framework's own `plugins/UltiTools/config.yml` `language` key, observable via `/ul help` showing the localized-vs-English block — reloaded through `UltiTools#reloadPlugins()` calling `reloadConfig()` then `initLanguage()`, NOT through `reloadSelf()`, since the framework is not itself a plugin module in `PluginManager`'s list; (2) UltiChat's `config/chat.yml` `chat.format` key, observable via a plain chat message (the same technique `ultichat.uchat.reload` uses for its own `/uchat reload` case) — reloaded through `PluginManager#reload()` calling UltiChat's own `UltiToolsPlugin#reloadSelf()`, a genuinely distinct code path from the framework's own reload above; this row is capped at proving these two named, differently-implemented fixtures both pick up their new values in one `/ul reload` pass, not literally all sixteen possible modules, and makes no claim beyond them; the server MUST be running with `language: zh` (NOT `en`) immediately before this row's own edit, so flipping it to `en` is a genuine state change rather than a no-op that would let `/ul help` show the English block even if `reloadConfig()`/`initLanguage()` never ran | Confirm `/ul help` currently shows the localized `lang/zh.json` block (proving the pre-edit state is genuinely non-English); edit both named fixtures directly on disk (`config.yml`'s `language` to `en`, `chat.yml`'s `chat.format` to a distinguishable value); run `/ul reload` once; then run `/ul help` and send a chat message to surface each fixture's new value | Before the edit, `/ul help` shows the Chinese block, not the English one; the framework's own `reloadConfig()`/`initLanguage()` pass and UltiChat's own `reloadSelf()` both run with no exception in the console; a module list taken with `/ul list` immediately after is unchanged; `/ul help` NOW shows the new English block (a genuine change from the confirmed pre-edit Chinese state, not a vacuous match) AND the sent chat message is formatted with the new `chat.format` value — either fixture failing to pick up its new value is rejected, since it cannot distinguish "every module reloaded" from "only one did" | server | |
| ultitools.ul.reload-module | `language: en` in config.yml; a loaded module named `<name>` (positive case: an existing module with the same kind of on-disk-value/command pairing as `ultitools.ul.reload`; negative case below: a name that does not exist) | Edit the named module's on-disk value, run `/ul reload <name>`, then run the module's command surfacing that value. Negative case: run `/ul reload does-not-exist` | Positive: chat/console line reads `Module <name> has been reloaded` (green), AND the module's command output reflects the new on-disk value. Negative: chat/console line reads `does-not-exist is not a loaded UltiTools module. Use /ul list to see loaded modules` (red) | server | |
| ultitools.ul.reload-log-line | `language: en` in config.yml (the Expected column asserts the literal English catalogue value; the shipped default resolves through lang/zh.json instead); same module precondition as ultitools.ul.list (appears after it); at least two modules currently loaded, with their names and count recorded from a fresh `/ul list` run immediately before this row's own steps | Run `/ul reload` from the server console and capture the full console output produced during that single command | The console output contains exactly one line per module recorded from the preceding `/ul list`, each reading, verbatim, `Module '<module name>' reloaded.` (the `lang/en.json` value of the framework's `RELOAD_LOG_MESSAGE_KEY`) — every recorded module's name appears in exactly one such line, no module is missing, and no module's name appears twice; the module's own log output (if any) is a separate matter this row makes no claim about | server | |

## /upm — plugin management

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.upm.check | `language: en` in config.yml; the startup update scan (triggered automatically at boot; watch the console until a terminal update-available/all-up-to-date line appears before running this row, rather than reading output at a fixed tick count) has already completed — `checkUpdates` only reads `UpdateManager`'s already-populated collections, it never itself queries or awaits the endpoint, so running this before that scan finishes reads the still-empty initial state; at least one update available (module or framework) — this is a deliberately-installed state, not the shipped default: install an older published version of a module or the framework via `upm install-version` (the update-family fixture recipe, `15-FIXTURES.md`); the older-version install alone does NOT reach this row's positive state — `UpdateManager#checkModuleUpdates`/`#checkFrameworkUpdate` populate `moduleUpdates`/`frameworkUpdate` ONLY during the one-time boot scan (`checkUpdatesSync`, called once from `UltiTools#onEnable`'s startup task); `/upm check` (`PluginInstallCommands`) never itself queries the endpoint or re-compares versions on demand, it only reads those already-populated fields via `getFrameworkUpdate()`/`getModuleUpdates()`. So after installing the older fixture, RESTART THE SERVER (a fresh boot scan) and again watch the console until a terminal update-available/all-up-to-date line appears before running this row. This row's precondition is established entirely by its own fixture install and restart — it does not depend on any other row's state or ordering. Within a batch, dispatch this row AFTER that older-version install AND its required restart | Run `/upm check` | Console/chat shows `Available updates:` followed by one `<name> <current> → <latest>` line per available update — `checkUpdates` formats the transition with a Unicode arrow `→`, not the ASCII sequence `->` | server | |
| ultitools.upm.check.neg-none | `language: en` in config.yml; the startup update scan (triggered automatically at boot; watch the console until a terminal line appears, as above) has already completed with network-capture-confirmed successful responses for every checked target, not merely an absence of warnings — a non-2xx response, a malformed envelope, or a parse error is silently indistinguishable from success by console output alone; no update available for any module or the framework — this IS the batch's default install state: within a batch, dispatch this row BEFORE the positive sibling row immediately above installs its own older-version fixture, since that install makes this row's own precondition false until the module or framework is updated back to latest | Run `/upm check` | Chat/console line reads `No updates available.` (green) | protocol | |
| ultitools.upm.help | `language: en` in config.yml | Run `/upm help` | Chat/console shows `========\|Plugin Install Help\|========` followed by eight usage lines, all green, starting with `/upm list [page] - View available plugin list` and ending with `/upm update all - Update all plugins`. This path has no `@CmdMapping` site — `BaseCommandExecutor#onCommand` matches the literal `help` argument and dispatches straight to `handleHelp` | server | |
| ultitools.upm.help.neg-bare | `language: en` in config.yml | Run bare `/upm` with no arguments | A preceding red `Unknown command, please enter /upm help for help` line, THEN the same help block as `ultitools.upm.help` — `matchMethod` returns null for zero args, so `onCommand` sends the unknown-command message before falling through to the same `handleHelp` call; the two invocations are not identical | server | |
| ultitools.upm.install | `language: en` in config.yml; `<plugin>` exists on UltiCloud and is not yet installed | Run `/upm install <plugin>` | Chat/console line reads `Installed! Please restart the server! Please be sure to delete the old version module!` (green); the module JAR now exists under `plugins/UltiTools/plugins/` | server | |
| ultitools.upm.install.neg-not-found | `language: en` in config.yml | Run `/upm install does-not-exist-on-ulticloud` | Chat/console line reads `Install Failed!` (red); no new file appears under `plugins/UltiTools/plugins/` | server | |
| ultitools.upm.install-version | `language: en` in config.yml; `<plugin>` has more than one version on UltiCloud; `<plugin>` is NOT already installed — `PluginInstallUtils#installPlugin(id, version)` only downloads the requested version and, unlike `updatePlugin`, never deletes an existing JAR, so an already-installed plugin would leave two JARs present and make the reported post-restart version unreliable | Run `/upm install <plugin> <version>`, choosing an older version | Chat/console line reads `Installed! Please restart the server! Please be sure to delete the old version module!` (green); the installed JAR reports `<version>` after restart | server | |
| ultitools.upm.install-version.neg-not-found | `language: en` in config.yml | Run `/upm install <plugin> 0.0.0-does-not-exist` | Chat/console line reads `Install Failed!` (red) | server | |
| ultitools.upm.list | `language: en` in config.yml; locate the catalogue page that lists a currently-loaded module — as a player, page through `/upm list <page>` starting from page 1 until an entry's install-state marker reads `installed`; that page, not necessarily page 1 (the live catalogue's own ordering is outside this project's control), is what the Steps below check for the marker-varies requirement — installing only downloads the JAR, `listPlugins` determines installed/not-installed from `PluginManager#getPluginList()` (the currently-loaded modules), so a newly-downloaded-but-not-yet-loaded module still reads `not installed`, and a fresh install must be followed by a server RESTART, confirmed via `/ul list` (or the startup log), before it reads `installed`; if the page found this way shows no entry marked `not installed` (all ten entries on it already loaded), uninstall one of the other entries on that same page by its resolved runtime name — use that entry's own uninstall click action from the player output, or the name shown in `/ul list`, NOT the catalogue `<id>` used for `/upm install`: `PluginInstallUtils#uninstallPlugin` matches only `getPluginName()`/the JAR's `plugin.yml` name, never the catalogue identifier — then RESTART THE SERVER, and confirm via `/ul list` that it no longer appears, so that page shows both states before running the Steps below | Run `/upm list` as a player, then separately as console; then, as a player, run `/upm list <page>` for the page located in the Preconditions above | The bare `/upm list` call (page 1) ends with `======== Page 1 ========` in both cases, listing AT LEAST ONE named entry — this is itself the proof the underlying `/plugin/list` request succeeded: `PluginInstallUtils#getPluginList` returns an empty list on any non-2xx or malformed response, and the for-loop below `listPlugins`'s header renders nothing for an empty list, so an empty page (header/footer only, zero entries) can never satisfy "AT LEAST ONE named entry" — no separate network capture is needed to distinguish "the catalogue loaded" from "the request failed and the loop ran zero times". As a player, each entry carries an install-state marker of literal text `installed` or `not installed` (each preceded by a single space in the actual output); console output carries no such marker at all, on page 1 or any other page — each entry lists only the name, an `Install Command：/upm install <id>` line, and the description. On the page located in the Preconditions above — the page listing the currently-loaded module, not necessarily page 1 — the player-branch output shows at least one entry reading `installed` and at least one reading `not installed`, proving the marker actually varies with real state rather than always printing the same value. Fixed #439: if that page includes an entry whose display name is `Economy` or `Menu` (the catalogue's own display names for the loaded `UltiTools-Economy`/`UltiTools-Menu` modules, which omit the `UltiTools-` prefix their runtime `plugin.yml` `name:` carries), that entry's marker reads `installed` — not `not installed` — while a catalogue entry for a module that is genuinely not loaded still reads `not installed`. (The rejection of a catalogue name that merely resembles a loaded module's without being it, e.g. one ending in `Pro`, is unit-covered only — `PluginInstallCommandsTest#resemblingButDifferentModuleIsRejected` — and is not asserted at this row's `server` layer, since no such entry is confirmed to exist on the live catalogue.) | server | |
| ultitools.upm.list-page | `language: en` in config.yml; the catalogue has more than one page; network-capture-confirm that the page-2 `/plugin/list` request returns an HTTP 2xx with at least one entry, for the same reason as `ultitools.upm.list` above — `PluginInstallUtils#getPluginList` returns an empty list on a non-2xx or malformed response, and `listPlugins` still prints the requested page's footer regardless, so an empty failed page 2 would satisfy this row's footer/differs-from-page-1 check without retrieving any page-2 data | Run `/upm list 2`, with the confirmed-successful page-2 response above | The footer line reads `======== Page 2 ========`, listing AT LEAST ONE named entry from the confirmed response, and the listed entries differ from page 1's | server | |
| ultitools.upm.list-page.neg-bad-page | `language: en` in config.yml | Run `/upm list not-a-number` | Chat/console line reads `Failed to parse 'not-a-number' as Integer` (red); no plugin listing is produced | server | |
| ultitools.upm.uninstall | `language: en` in config.yml; `<plugin>` is currently installed and listed by `/ul list`; you know the file name of its JAR in `plugins/UltiTools/plugins/` | Run `/upm uninstall <plugin>`, then list `plugins/UltiTools/plugins/` and run `/ul list`. Cleanup: stop the server, copy the module's JAR back into `plugins/UltiTools/plugins/`, start the server | The reply is a single green line reading exactly `Uninstalled! Every JAR file of the module has been deleted.` — no `File Location:` line and no instruction to delete anything by hand. The module's JAR is gone from `plugins/UltiTools/plugins/`, and `/ul list` no longer lists the module. A `File Location:` line, a surviving JAR, or the module still listed is `fail` | server | |
| ultitools.upm.uninstall.neg-not-found | `language: en` in config.yml | Run `/upm uninstall does-not-exist` | Chat/console line reads `Uninstall Failed! Please check if the spelling is correct!` (red) | server | |
| ultitools.upm.uninstall.neg-command-gone | Same precondition as ultitools.upm.uninstall (appears two rows above); `<plugin>` additionally has at least one of its own top-level commands registered, confirmed resolvable by running it once from the server console before this row's own steps (it must execute, not report "unknown command") | From the server console: run `<plugin>`'s own command once to confirm it currently resolves; run `/upm uninstall <plugin>`; then run the exact same command a second time | Before uninstall, the command executes normally. After `/upm uninstall <plugin>` reports success (per `ultitools.upm.uninstall`'s own Expected), running the identical command again does NOT execute. Instead the console prints Paper 1.21.11's own vanilla unknown-command error, verbatim: `Unknown or incomplete command. See below for error`, followed by a second line echoing the full input typed and ending in `<--[HERE]`. That `<--[HERE]` marker must sit at the very first character of the echoed input (Brigadier's unknown-root error) — a marker placed after the command's own label instead would mean the label still resolved and only an argument failed (a different error, `Incorrect argument for command`), which would NOT satisfy this row. Only the first-character-marker form proves the module's command was actually unregistered from Bukkit's command map on unload (D-01), not merely reported uninstalled while still live | server | |
| ultitools.upm.uninstall.tasks-stop | Every precondition of ultitools.upm.uninstall (appears earlier in this file), and `<plugin>` is a module with a visible repeating behaviour of its own — one driven by a `@Scheduled` method or a repeating task, such as a scoreboard or action-bar that refreshes on its own every few seconds — observed refreshing before this row's steps | Watch the module's repeating output for at least three of its refresh intervals; run `/upm uninstall <plugin>`; keep watching the same output for at least three more intervals without restarting the server. Cleanup: as in ultitools.upm.uninstall | The repeating output stops at the uninstall and does not resume before a restart, and no exception from that module appears in the console afterwards. Output that keeps refreshing after the reply, or a repeating exception from the uninstalled module, is `fail` | server | |
| ultitools.upm.uninstall.no-jar | Every precondition of ultitools.upm.uninstall (appears earlier in this file), and the server host allows moving an open file (any Linux host does) | While the server is running, move `<plugin>`'s JAR out of `plugins/UltiTools/plugins/` to a directory outside it; then run `/upm uninstall <plugin>`. Cleanup: move the JAR back and restart | The reply is a single yellow line reading `The module was unloaded, but no JAR file of it was found in <plugins folder path>/plugins.` and `/ul list` no longer lists the module. A reply mentioning spelling, or a green success line, is `fail` | server | |
| ultitools.upm.update | `language: en` in config.yml; a named module has an update available (appears after ultitools.upm.check) | Run `/upm update <module-name>` | Chat/console shows `Updating <module-name>...` then `Update successful! Please restart the server to apply.` (both, one yellow one green) | server | |
| ultitools.upm.update-all | `language: en` in config.yml; at least two modules have updates available | Run `/upm update all` | `PluginInstallCommands#updateAllPlugins` — a separate branch from the single-module path above, with its own iteration and partial-failure accounting — shows one `Updating <module-name>...` line per module with an update (yellow), then `All updates complete! <N> succeeded, <N> failed. Please restart.` (green), with `<N>` matching the actual update count for each outcome | server | |
| ultitools.upm.update.neg-no-update | `language: en` in config.yml; the named module has no update available | Run `/upm update <module-name>` | Chat/console line reads `No updates available.` (red — this branch of `updatePlugin` uses `ChatColor.RED`, unlike `checkUpdates`'s green use of the same string) | server | |
| ultitools.upm.versions | `language: en` in config.yml; `<plugin>` exists on UltiCloud | Run `/upm versions <plugin>` | Numbered version list is shown, the last entry followed by a line reading `Install Command：/upm install <plugin> [version]` indented by three leading spaces (full-width colon `：` and capital `Command`, matching the framework's own translation string verbatim — not an ASCII `Install command:`) | server | |
| ultitools.upm.versions.neg-not-found | `language: en` in config.yml | Run `/upm versions does-not-exist-on-ulticloud` | Chat/console line reads `Could not fetch the version list!` (red) | server | |

## /ulticloud — cloud authentication

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.ulticloud.help | Console access | Run `/ulticloud help` | Console shows `=== UltiCloud Commands ===` (aqua) followed by `/ulticloud login - Authenticate with UltiCloud`, `/ulticloud logout - Clear saved credentials`, `/ulticloud status - Show connection status` (white command, gray description). This path has no `@CmdMapping` site — same `BaseCommandExecutor#onCommand` dispatch as `ultitools.upm.help` | server | |
| ultitools.ulticloud.help.neg-bare | Console access | Run bare `/ulticloud` with no arguments | A preceding red `Unknown command, please enter /ulticloud help for help` line, THEN the same help block as `ultitools.ulticloud.help` — same `matchMethod`-returns-null fallthrough as `ultitools.upm.help.neg-bare`; the two invocations are not identical | server | |
| ultitools.ulticloud.login | Console access; this server not currently authenticated with UltiCloud; the maintainer's own UltiCloud account and browser access to open the printed link; an authorized UltiCloud session under the maintainer's delegated authorization | Run `/ulticloud login` from console, then open the printed URL and complete the login in a browser within 5 minutes | Console prints a boxed `Open this URL in your browser to login:` block with a real URL, then, once the poller (`CloudAuthManager#pollLoginStatusOnce`) observes `status: completed`, console logs `UltiCloud login successful! Welcome, <username>!` at INFO — this confirmation is a background log line, not a reply to the command sender, because it fires from the async poll executor, not from `CloudLoginCommand#login` itself; `/ulticloud status` (see below) then reads `UltiCloud: Connected as <username>` | server | |
| ultitools.ulticloud.login.neg-already-logged-in | This server already authenticated with UltiCloud; an authorized UltiCloud session under the maintainer's delegated authorization | Run `/ulticloud login` | Chat/console line reads `Already logged in to UltiCloud. Use /ulticloud logout first to re-login.` (yellow) | server | |
| ultitools.ulticloud.login.neg-rate-limited | This server not currently authenticated with UltiCloud — `login()` checks `CloudAuthManager.hasValidToken()` before the rate limiter and returns the `Already logged in` message instead, so an authenticated server never reaches this path on either call; run `/ulticloud login` once first so a login attempt has just been made | Run `/ulticloud login` again immediately | Chat/console line reads `Please wait <N> seconds before trying again.` (red) | server | |
| ultitools.ulticloud.logout | This server currently authenticated with UltiCloud (appears after ultitools.ulticloud.login); an authorized UltiCloud session under the maintainer's delegated authorization | Run `/ulticloud logout` | Chat/console shows `Successfully logged out of UltiCloud. Cloud features are now disabled.` then `Use /ulticloud login to re-authenticate.` (green, then gray) | server | |
| ultitools.ulticloud.logout.neg-not-logged-in | This server not currently authenticated with UltiCloud | Run `/ulticloud logout` | Chat/console shows `Not currently logged in to UltiCloud.` then `Cloud features have been stopped regardless.` (yellow, then gray) | server | |
| ultitools.ulticloud.status | This server authenticated with UltiCloud under the maintainer's own account (appears after ultitools.ulticloud.login); an authorized UltiCloud session under the maintainer's delegated authorization | Run `/ulticloud status` | Chat/console line reads `UltiCloud: Connected as <the maintainer's real UltiCloud username>` (green), naming the real connected account rather than a placeholder | server | |
| ultitools.ulticloud.status.neg-not-connected | This server not currently authenticated with UltiCloud | Run `/ulticloud status` | Chat/console shows `UltiCloud: Not connected` then `Use /ulticloud login to authenticate.` (yellow, then gray) | server | |
| ultitools.ulticloud.logout.neg-mid-poll | `language: en` in config.yml; console access; this server not currently authenticated with UltiCloud; `/ulticloud login` (see `ultitools.ulticloud.login` above) has just been run and its printed URL has deliberately NOT been opened yet, so the 5-minute magic-link poll is still in flight; an authorized UltiCloud session under the maintainer's delegated authorization | While the poll from the preceding `/ulticloud login` is still running, run `/ulticloud logout`. Then open the printed URL and complete the login in the browser (or let the 5-minute window elapse), and only afterward run `/ulticloud status` | `/ulticloud logout` shows `Successfully logged out of UltiCloud. Cloud features are now disabled.` then `Use /ulticloud login to re-authenticate.` (green, then gray) — same lines as `ultitools.ulticloud.logout`. Completing the magic-link login afterward (or letting it time out) must NOT reactivate the server: no `UltiCloud login successful!` line may appear in console, and the subsequent `/ulticloud status` must still read `UltiCloud: Not connected` then `Use /ulticloud login to authenticate.` (yellow, then gray) — a server that silently became connected anyway is exactly the pre-fix race issue #298 named: a login result committed after logout used to reactivate cloud features because nothing tied the in-flight poll to the session logout had already replaced | server | |

## Boot sequence and listeners

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.boot.plugin-load-order | At least two modules installed with a declared hard `@PluginDependency` between them; the server is NOT launched with `-Dultitools.useLegacyPluginLoading=true` (that flag bypasses dependency resolution entirely, producing a different console signature than this row's Expected) | Restart the server and read the console output during startup | Console shows `[UltiTools-API] Plugin load order resolved successfully.`, and the dependent module's own `onEnable` log line appears after its dependency's | server | |
| ultitools.boot.plugin-load-order.neg-missing | One installed module declares a hard `@PluginDependency` on a module name that is not installed, and no other installed module depends on it; the server is NOT launched with `-Dultitools.useLegacyPluginLoading=true` | Restart the server and read the console output during startup | Console shows `[UltiTools-API] A required plugin dependency is missing.` followed by the resolver's own message naming the missing dependency; every module with no dependency path (direct or transitive) to it still loads and appears in `/ul list`. `PluginDependencyResolver#expandForward` excludes not just the module with the missing dependency but every module that transitively depends on it — a module `C` that depends on `A`, which depends on the missing module, is excluded too, even though `C` never itself declares the missing dependency | server | |
| ultitools.boot.plugin-load-order.neg-circular | Two installed modules declare a hard `@PluginDependency` on each other, and no other installed module depends on either; the server is NOT launched with `-Dultitools.useLegacyPluginLoading=true` | Restart the server and read the console output during startup | Console shows `[UltiTools-API] Circular dependency detected among plugins.` followed by a `Loop: A -> B -> A`-shaped line naming both modules; every module with no dependency path (direct or transitive) into the cycle still loads. Kahn's algorithm never assigns a node downstream of the cycle an in-degree of 0, so a module that depends on a cycle member is excluded too, not just the two modules forming the cycle itself | server | |
| ultitools.boot.plugin-load-order.neg-missing-multiple | One installed module declares THREE missing hard `@PluginDependency` targets, none of which are installed, and no other installed module depends on it; the server is NOT launched with `-Dultitools.useLegacyPluginLoading=true` | Restart the server and read the console output during startup | Console shows `[UltiTools-API] A required plugin dependency is missing.` followed by ONE line naming ALL THREE missing dependencies for that module in a single plural-form clause — `[UltiTools-API] Plugin '<Module>' requires dependencies '<A>', '<B>', '<C>' which are not available` — not three separate restarts each revealing one more name. Framework#361, fixed | server | |
| ultitools.boot.plugin-load-order.neg-duplicate-name | Two installed modules' `plugin.yml` files declare the SAME `name:` value; neither depends on the other, and the server is NOT launched with `-Dultitools.useLegacyPluginLoading=true` | Restart the server and read the console output during startup | Console shows a WARNING line `[UltiTools-API] Multiple modules declare the same plugin.yml name '<Name>': [<ModuleA>, <ModuleB>]. Dependency resolution will resolve this name to '<ModuleA>' (the module discovered first); rename one module's plugin.yml name: to remove the ambiguity.` — naming both modules and the shared name; BOTH modules still load (this is a report, not a refusal). Framework#361, fixed | server | |
| ultitools.boot.plugin-load-order-legacy | Server launched with `-Dultitools.useLegacyPluginLoading=true` (modeled on Paper's own `-Dpaper.useLegacyPluginLoading=true` precedent); at least two modules with a declared hard `@PluginDependency`, or a missing/circular one | Restart the server with that flag set and read the console output during startup | `PluginManager#sortPluginsByDependencies` skips `PluginDependencyResolver` entirely and returns filesystem order; console shows `[UltiTools-API] Legacy unsorted plugin load order is ACTIVE because -Dultitools.useLegacyPluginLoading=true is set on the command line. Dependency resolution is skipped entirely - modules load in filesystem order and may fail to initialize if they rely on load order.` — none of the three rows above's success/missing/circular messages appear, and a module may fail to initialize regardless of any declared dependency | server | |
| ultitools.boot.update-check | `language: en` in config.yml; server has outbound access to the update-check endpoint; a newer framework or module version exists | Restart the server. `UltiTools#checkUpdatesSync` runs on a `runTaskAsynchronously` task making blocking HTTP requests for the framework version plus one per installed module, so it does NOT finish within the same tick that starts it — watch the console until a terminal line (an update-available/all-up-to-date line, or a `Failed to check ...` warning) appears, rather than reading output at a fixed tick count; a bare `[UltiTools-API] Checking for updates...` with nothing after it is the check still in flight, not a fail | Console shows `[UltiTools-API] Checking for updates...`; if a newer framework version exists, `[UltiTools-API] UltiTools-API update available: <latest> (current: <current>)` then `[UltiTools-API] Download URL: https://github.com/UltiKits/UltiTools-Reborn/releases/latest`; if any module updates exist, `[UltiTools-API] Module updates available (<N>):` then one `[UltiTools-API]   <module> <current> -> <latest>` line per module — this startup log path is not i18n'd for the arrow, so it is the ASCII `->`, unlike `/upm check`'s chat output which uses `→` | server | |
| ultitools.boot.update-check.neg-none | `language: en` in config.yml; server has outbound access to the update-check endpoint; no newer framework or module version exists; a network capture (or equivalent proxy/log evidence) confirms the update-check HTTP request actually completed with a `200` response and a well-formed `{"code": "200", "data": ...}` envelope for both the framework and every installed module — `VersionUtils#getUltiToolsNewestVersion` and `PluginInstallUtils#getPluginLatestVersion` return `null` silently (no warning logged at all) for a non-2xx response, a malformed envelope, missing `data`, or a parse error, so the absence of a `Failed to check ...` warning line is NOT sufficient evidence the query succeeded — those two failure classes look identical from the console alone | Restart the server. As with `ultitools.boot.update-check`, `checkUpdatesSync` runs asynchronously and involves blocking HTTP calls, so it does not complete within the same tick it starts on — watch the console until a terminal line appears rather than reading output at a fixed tick count | Console shows `[UltiTools-API] All plugins are up to date!` | protocol | |
| ultitools.listener.placeholderapi-bridge | PlaceholderAPI installed and enabled; a UltiTools-provided placeholder expansion (e.g. `player`) not yet registered | Join the server as any player | Within about 60 seconds the console shows the framework dispatching `papi ecloud download Player` (and, if any expansion needed downloading, a later `papi reload`) from the console sender | server | |
| ultitools.listener.update-notify | An OP player joins after ultitools.boot.update-check found at least one update, and has not yet been notified this session | Join the server as an OP player | Chat shows `[UltiTools] <N> update(s) available. Run /upm check for details.` exactly once for that connection — the green `[UltiTools]` prefix ends there; everything from `<N>` through `for details.` renders yellow, because `ChatColor.YELLOW` is set once at the count and persists to end of line (per `UpdateJoinListener.java:45-46`), not merely the count itself | server | |
| ultitools.listener.update-notify.neg-repeat-after-quit | Same precondition as ultitools.listener.update-notify, then the same player quits | Rejoin as the same OP player, without restarting the server | The notification is NOT sent again — `notifiedPlayers` is no longer `@PlayerCache`-annotated, so `PlayerCacheManager#onPlayerQuit` does not clear it on that player's quit; the mark persists for the lifetime of the `UpdateManager` instance, which is exactly one server run, matching `UpdateJoinListener`'s own javadoc promise of "once per server session". Framework#431, fixed. Restarting the server (a genuinely new `UpdateManager` instance) DOES notify that OP player again on their next join — the mark does not survive past the run it was set in | server | |

## Economy

All five rows share one real, verifiable trigger fixture rather than an artificial call site:
UltiEssentials' `DeathPunishListener`, whose `deathPunishMoneyEnabled` config key gates a call to
`EconomyUtils.isAvailable()` on every player death — the same check every other economy-consuming
module in this codebase performs before touching a balance (measured: `UltiEssentials`,
`UltiKits`, `UltiRemoteBag` all gate the same way), so a warning that fires here is proven against
a real module's real call pattern, not a synthetic one.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.economy.report-startup-state.neg-vault-absent | `language: en` in config.yml; no plugin named `Vault` installed on the server; UltiEssentials installed and enabled with `deathPunishMoneyEnabled: true` in its own config | Restart the server and read the console startup log; then let any player die once | Console startup log shows exactly `[UltiTools-API] Vault not installed - economy service unavailable.`; every installed module — including UltiEssentials — still appears in `/ul list` and finishes enabling, proving the framework's own bootstrap does not crash on a server with no Vault plugin (issue #451, previously a total load failure here); after the death, console shows exactly one WARNING line reading `[UltiTools-API] Module 'UltiEssentials' requested the economy service, but Vault is not installed on this server. This is the server's environment, not a defect in UltiTools or in 'UltiEssentials'. Install Vault and an economy plugin (e.g. EssentialsX) that registers a Vault economy provider to enable economy features.` | server | |
| ultitools.economy.report-startup-state.neg-no-provider | `language: en` in config.yml; Vault plugin installed and enabled; no economy plugin (e.g. EssentialsX) installed, so no `Economy` provider is registered with Vault; UltiEssentials installed and enabled with `deathPunishMoneyEnabled: true` in its own config | Restart the server and read the console startup log; then let any player die once | Console startup log shows exactly `[UltiTools-API] Vault detected, but no economy provider is registered - economy service unavailable.`; every installed module, including Vault itself and UltiEssentials, finishes enabling; after the death, console shows exactly one WARNING line reading `[UltiTools-API] Module 'UltiEssentials' requested the economy service, but Vault is installed, but no economy provider is registered with it. This is the server's environment, not a defect in UltiTools or in 'UltiEssentials'. Install Vault and an economy plugin (e.g. EssentialsX) that registers a Vault economy provider to enable economy features.` | server | |
| ultitools.economy.report-startup-state | `language: en` in config.yml; Vault plugin installed and enabled; an economy plugin (e.g. EssentialsX) installed and enabled, registering an `Economy` provider with Vault; UltiEssentials installed and enabled with `deathPunishMoneyEnabled: true` in its own config | Restart the server and read the console startup log; then let any player die once, checking the player's balance via the registered economy provider's own command (e.g. `/balance` for EssentialsX) immediately before and immediately after the death | Console startup log shows `[UltiTools-API] Hooked into Vault, economy provider: <provider name>.` naming the actual registered provider; after the death, NO `[UltiTools-API] Module '...' requested the economy service` WARNING line appears for UltiEssentials, and the player's balance after the death is strictly lower than before it — the death-punish money deduction proceeds normally instead of being silently skipped | server | |
| ultitools.economy.report-unavailable.neg-vault-absent | `language: en` in config.yml; no plugin named `Vault` installed on the server; UltiEssentials installed and enabled with `deathPunishMoneyEnabled: true` in its own config; the server has just been (re)started into this state and no economy request has been made by UltiEssentials since that restart, so `EconomyUtils`'s per-module warned-once state for UltiEssentials (`warnedModules`) is still clear — the same environment as `ultitools.economy.report-startup-state.neg-vault-absent` above, exercised as a fresh, self-contained restart rather than by relying on that row's own death having already fired the warning | Let a player die once, then let a player (the same one or a different one) die a second time in the same server session, without restarting in between | After the first death, console shows exactly one WARNING line reading `[UltiTools-API] Module 'UltiEssentials' requested the economy service, but Vault is not installed on this server. This is the server's environment, not a defect in UltiTools or in 'UltiEssentials'. Install Vault and an economy plugin (e.g. EssentialsX) that registers a Vault economy provider to enable economy features.`; after the second death, NO further WARNING line for UltiEssentials appears — `EconomyUtils.reportEconomyStateIfUnavailable` warns each calling module at most once per server session (`warnedModules` is never cleared outside of `EconomyUtils#reset()`, which only test code calls), by design, not a defect | server | |
| ultitools.economy.report-unavailable.neg-no-provider | `language: en` in config.yml; Vault plugin installed and enabled; no economy plugin (e.g. EssentialsX) installed, so no `Economy` provider is registered with Vault; UltiEssentials installed and enabled with `deathPunishMoneyEnabled: true` in its own config; the server has just been (re)started into this state and no economy request has been made by UltiEssentials since that restart, so `EconomyUtils`'s per-module warned-once state for UltiEssentials (`warnedModules`) is still clear — the same environment as `ultitools.economy.report-startup-state.neg-no-provider` above, exercised as a fresh, self-contained restart rather than by relying on that row's own death having already fired the warning | Let a player die once, then let a player (the same one or a different one) die a second time in the same server session, without restarting in between | After the first death, console shows exactly one WARNING line reading `[UltiTools-API] Module 'UltiEssentials' requested the economy service, but Vault is installed, but no economy provider is registered with it. This is the server's environment, not a defect in UltiTools or in 'UltiEssentials'. Install Vault and an economy plugin (e.g. EssentialsX) that registers a Vault economy provider to enable economy features.`; after the second death, NO further WARNING line for UltiEssentials appears — `EconomyUtils.reportEconomyStateIfUnavailable` warns each calling module at most once per server session (`warnedModules` is never cleared outside of `EconomyUtils#reset()`, which only test code calls), by design, not a defect | server | |

## Scheduled tasks

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.task.player-cache-sweep | A module bean using `@PlayerCache` AND implementing `PlayerCacheManager.ExpiringPlayerCache` is registered, with at least one expired entry from a player who is no longer online — `sweepExpiredEntries` invokes `sweepExpired()` only on beans implementing that interface; a bean without it is never swept by this task | Wait for one 5-minute sweep interval | No `ConcurrentModificationException` or `Error running expiry sweep for <class>` warning appears in the console under this normal-operation pass; the bean's own `sweepExpired()` runs and the expired entry is gone from its tracked state afterward | server | |
| ultitools.task.player-cache-sweep.neg-hook-throws | Same as the row above, but the bean's `sweepExpired()` implementation is made to throw during the sweep pass (a forced-failure build/config, not normal operation) | Wait for one 5-minute sweep interval | An `Error running expiry sweep for <class>` WARNING appears in the console, naming the throwing bean's class; that bean's expired entry is NOT removed from its tracked state (the hook aborted before completing), but the sweep pass itself continues — a different bean's own `sweepExpired()` in the same pass, if registered, still runs and its own expired entry is still removed | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.storage.backend-select | `language: en` in config.yml; `datasource.type: sqlite` in config.yml (shipped default); at least one loaded module has performed a `@Table`-backed data operation since the last restart — `SQLiteDataStore` creates its `.db` file lazily from `getOperator`, never at startup, so a server with zero data operations has no file to find | Restart the server, trigger one module data operation, and read the startup console log | Console shows `Data Storage Method: sqlite`. The `.db` file's location depends on which `getOperator` overload the module used: an internal `UltiToolsPlugin` module (`getOperator(UltiToolsPlugin, Class)`, the common case) creates `plugins/UltiTools/sqliteDB/<plugin-name>.db` — NOT a file under that module's own `plugins/<ModuleName>/` data folder; only a plain Bukkit plugin using the External Plugin API (`getOperator(File, Class)`) creates `data.db` directly under its own data folder | server | |
| ultitools.storage.backend-select.neg-fallback | `datasource.type: mysql` AND `mysql.enable: true` in config.yml, but `mysql.host` points at an unreachable server — this combination selects `DataStoreManager#reportBackendSelection`'s connection-failed branch specifically; `mysql.enable: false` (the shipped default) with `datasource.type: mysql` reaches the same fallback through a different, `mysql.enable`-is-false reason and is not what this row exercises | Restart the server and read the startup console log | Two SEVERE lines: `Data store downgraded: datasource.type in config.yml is 'mysql', but 'json' is actually in use. Reason: MySQL connection failed, the data source is unavailable (see the connection error above)`, then `Player data will be read from and written to the 'json' store. Data is not shared between backends - if this is not what you want, stop the server, fix config.yml and restart.` — the INFO-level `Data Storage Method: json` startup line alone would NOT satisfy this row, since it never mentions that config asked for `mysql` (issue #183) | server | |
| ultitools.storage.restart-survival | A row written through a `DataOperator` while the server is up (any module command backed by `@Table`) | Stop the server completely, then start it again, then read the same row back through the same module command | The same value is returned after restart, in whichever backend `ultitools.storage.backend-select` is currently active | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.language.select | `language: en` in config.yml, server restarted after the change | Run `/ul help`; separately, with the server running under `language: zh` and NOT freshly restarted, edit `language: "en"` and run `/ul reload` (no restart) instead, then run `/ul help` again | Both paths produce the English text `=== UltiTools Commands ===` block, not the Chinese equivalent — `UltiTools#reloadPlugins`'s `initLanguage()` call re-applies the language without a restart just as effectively as the restart path does, unlike `datasource.type` | server | |
| ultitools.language.select.neg-default | `language: zh` in config.yml (shipped default), server restarted after the change | Run `/ul help` | Output is the localized block from `lang/zh.json`'s help-text key, visibly different from the English `=== UltiTools Commands ===` block above, not a Chinese/English mix | server | |
| ultitools.language.file-refresh | `language: en` in config.yml; a loaded `<module>` (e.g. UltiChat) has already started at least once with its currently-installed jar, so `plugins/UltiTools/pluginConfig/<module>/lang/en.json` exists and `plugins/UltiTools/pluginConfig/<module>/.ultitools-resource-hashes.json` records its hash for `lang/en.json`; that on-disk `lang/en.json` has NOT been hand-edited since; a rebuilt `<module>` jar is staged whose own `src/main/resources/lang/en.json` differs byte-for-byte from the currently-extracted one (e.g. a wording change to an existing key) | Stop the server, replace the installed `<module>` jar with the rebuilt one, restart | `plugins/UltiTools/pluginConfig/<module>/lang/en.json` on disk is now byte-for-byte identical to the NEW jar's bundled `lang/en.json` (confirm with a checksum or `diff` against the new jar's extracted copy), NOT the previously-extracted content; the startup console log contains a line whose text (module-name-derived console prefixing aside) is `[<module>] Language file 'lang/en.json' for module '<module>' was not modified since it was extracted and has been updated to the current bundled version.` (the exact literal `PluginLogger.info` argument this code path builds — `UltiToolsPlugin#resolveLanguageWithProvenance`) | server | |
| ultitools.language.file-preserve | Same base setup as `ultitools.language.file-refresh` (appears after it), except `plugins/UltiTools/pluginConfig/<module>/lang/en.json` is hand-edited (any change, so its bytes no longer match the recorded provenance hash) BEFORE the rebuilt jar is staged; the rebuilt `<module>` jar's `lang/en.json` changes the `String.format` placeholder count of one existing key `<key>` relative to the currently-extracted version (e.g. `"Hi %s"` becoming `"Hi %s, you have %s items"`, adding a second `%s` parameter to a single-parameter message) — `<key>` is left untouched by the hand-edit above, isolating the arity check from the unrelated hand-edited key | Stop the server, replace the installed `<module>` jar with the rebuilt one, restart | `plugins/UltiTools/pluginConfig/<module>/lang/en.json` on disk is byte-for-byte unchanged from the hand-edited version (confirm with a checksum taken before and after the restart) — the file is NOT overwritten; the startup console log contains a line whose text (module-name-derived console prefixing aside) is `[<module>] Language key '<key>' in 'lang/en.json' for module '<module>' has a different placeholder count than the current bundled version; using the current version's value for this key.` (the exact literal `PluginLogger.warn` argument this code path builds — `UltiToolsPlugin#applyPlaceholderArityOverride`); triggering `<key>`'s message afterwards (e.g. via the module's own command or event that renders it) shows the NEW jar's placeholder count (both `%s` parameters substituted), not the old single-placeholder form, while every OTHER hand-edited key in the same file still renders with the hand-edited text | server | |

## Panel capabilities

Every row below is exercised by toggling the named `ultipanel.capabilities.*` key, then sending
the corresponding panel message type (or reading `RemoteActionLog` for the same effect without a
live panel session). The refusal text checked below is `Capability#configurableRefusal`'s literal
wording, produced by `Capability#refusalMessage()`.

**How the toggle takes effect differs by capability, and only five of the eight can use `/ul
reload`.** `commands`, `file-read`, `file-write`, `file-delete`, and `server-properties` are
gated per-request — `PluginInitiationUtils#dispatchWithCapabilityGate` calls
`Capability#isEnabled()` fresh on every inbound message, which reads the live (already-reloaded)
`config.yml`, so `/ul reload` is sufficient. `monitoring`, `logs`, and `player-events` are gated
once, at connect time, inside `PluginInitiationUtils#wireManagers` — `/ul reload`'s
`UltiTools#reloadPlugins` never calls `wireManagers`, so toggling one of these three requires
either a full server restart, or an explicit `/ulticloud logout` followed by `/ulticloud login`
(the only two paths that reach `wireManagers` after tearing the prior state down). An ordinary
WebSocket reconnect alone does NOT clear an already-active one of these three: `wireManagers`
only skips calling `startMonitoring()`/`initialize()` when the capability is now disabled, it
never calls the matching stop — `ServerMonitorManager#setWebSocketClient` runs unconditionally
and any already-running scheduled task or listener from before stays active. `disableCloud()`
(the method `/ulticloud logout` calls) is the one path that actually calls `stopMonitoring()`
before the next login re-runs `wireManagers`.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.capability.commands | `ultipanel.capabilities.commands: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send an `execute_command` panel message through the real WebSocket connection — NOT a direct `CommandExecutionManager` call, which bypasses `PluginInitiationUtils#dispatchWithCapabilityGate` and would let this row pass even if the actual remote route were mis-gated or refusing every request | The command executes and `RemoteActionLog` records an `ALLOWED` verdict for the `execute_command` action | server | |
| ultitools.capability.commands.neg-disabled | `ultipanel.capabilities.commands: false` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Send an `execute_command` panel message | The command is refused; `RemoteActionLog` records a `DENIED` verdict whose reason is `Blocked by policy — edit 'ultipanel.capabilities.commands' in plugins/UltiTools/config.yml to change this.` | server | |
| ultitools.capability.file-delete | `ultipanel.capabilities.file-delete: true`; the target is an EXISTING, disposable regular file inside an editable root with an allowed extension and not a protected name (e.g. a temporary plain `.txt` file created for this row, not a `.jar`/`.class` or a credential-bearing filename) — `FileOperationManager#deniedUnconditionally` refuses those even inside an editable root, and a nonexistent target is refused by `handleDeleteOperation` regardless of this capability; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-delete panel message for that file | The file is deleted and `RemoteActionLog` records an `ALLOWED` verdict | server | |
| ultitools.capability.file-delete.neg-disabled | `ultipanel.capabilities.file-delete: false` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-delete panel message | The delete is refused with the reason naming `ultipanel.capabilities.file-delete` | server | |
| ultitools.capability.file-delete.recursive | `ultipanel.capabilities.file-delete: true`; the target is an EXISTING, disposable, non-empty directory inside an editable root, containing at least one file, and no descendant at any depth is unconditionally protected (no credential-bearing filename, no `.jar`/`.class`, no other basename/extension `FileOperationManager#deniedUnconditionally` refuses) — `#deniedAsAncestorOfProtectedPath` walks every descendant before deletion and refuses if any is; the request's data carries `recursive` as the JSON boolean primitive `true` (appears after `ultitools.capability.file-delete`, whose single-file precondition this row's directory target replaces); an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-delete panel message for that directory with `recursive: true` | The directory and everything under it is deleted and `RemoteActionLog` records an `ALLOWED` verdict | server | |
| ultitools.capability.file-delete.neg-recursive-unset | Same directory-target precondition as `ultitools.capability.file-delete.recursive` (appears after it), except the request's data either omits the `recursive` field entirely, or carries it as the JSON boolean `false`, or as the JSON string `"false"` — `#requireRecursiveFlag` treats a present-but-non-boolean value as absent rather than coercing it, so all three are refused identically; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-delete panel message for that directory without `recursive` set to the boolean `true` (try each of the three variants above) | The delete is refused with the reason `deleting a directory requires the request's 'recursive' field to be the boolean true`; the directory and its contents still exist afterward | server | |
| ultitools.capability.file-read | `ultipanel.capabilities.file-read: true` (shipped default); the target is an EXISTING regular file inside an editable root with an allowed extension and not a protected name (e.g. a plain `.txt` file, not a `.jar`/`.class` or a credential-bearing filename — `FileOperationManager#deniedUnconditionally` refuses those even inside an editable root, and a nonexistent path or a directory is rejected by the read handler regardless of the root check, independent of this capability); an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-read panel message for that file | The file contents are returned | server | |
| ultitools.capability.file-read.neg-disabled | `ultipanel.capabilities.file-read: false`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-read panel message | The read is refused with the reason naming `ultipanel.capabilities.file-read` | server | |
| ultitools.capability.file-write | `ultipanel.capabilities.file-write: true`; the target path is inside an editable root, does NOT already exist as a protected name, and has an allowed extension (e.g. a new plain `.txt` file, not `.jar`/`.class` or a credential-bearing filename) — `FileOperationManager#deniedUnconditionally` refuses a blocked filename/extension/credential pattern even inside an editable root, ahead of this capability's own gate; a non-null content payload is supplied, since `handleWriteOperation` separately rejects `Content cannot be null`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-write/upload panel message for that path with a non-null content payload | The file is written and `RemoteActionLog` records an `ALLOWED` verdict | server | |
| ultitools.capability.file-write.neg-disabled | `ultipanel.capabilities.file-write: false` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-write panel message | The write is refused with the reason naming `ultipanel.capabilities.file-write` | server | |
| ultitools.capability.logs | `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Open a log-stream panel session | Live console log lines stream to the panel | server | |
| ultitools.capability.logs.neg-disabled | `ultipanel.capabilities.logs: false`; an authorized UltiCloud session under the maintainer's delegated authorization | Open a log-stream panel session | The stream is refused with the reason naming `ultipanel.capabilities.logs` | server | |
| ultitools.capability.monitoring | `ultipanel.capabilities.monitoring: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Observe the panel connection for one `batch_update` cycle | `status` and `metrics` fields are present every cycle | server | |
| ultitools.capability.monitoring.neg-disabled | `ultipanel.capabilities.monitoring: false`; an authorized UltiCloud session under the maintainer's delegated authorization | Observe the panel connection for one `batch_update` cycle | The server reads as offline to the panel — this capability is the panel's only "server is alive" signal | server | |
| ultitools.capability.player-events | `ultipanel.capabilities.player-events: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Join/quit/chat as a player while a panel session is open | The corresponding `player_event` message reaches the panel, `data.event_type` = `player_join`/`player_quit`/`player_chat` | server | |
| ultitools.capability.player-events.neg-disabled | `ultipanel.capabilities.player-events: false`; an authorized UltiCloud session under the maintainer's delegated authorization | Join/quit/chat as a player while a panel session is open | No `player_event` message reaches the panel for that action | server | |
| ultitools.capability.server-properties | `ultipanel.capabilities.server-properties: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a `server_properties` edit for a key on `ServerPropertiesManager`'s safe-key list | The key is written to `server.properties` and the response reports success | server | |
| ultitools.capability.server-properties.neg-disabled | `ultipanel.capabilities.server-properties: false` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization | Send a `server_properties` edit | The edit is refused with the reason naming `ultipanel.capabilities.server-properties` | server | |

## Live log stream controls

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.remote.log-stream-pause | `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; a log-stream session already open (`ultitools.capability.logs`, appears after it) | Send `log_stream` `action: "pause"` for the open session's `clientId` | The panel receives an error response naming the action ("pause"/"resume") "not supported", stating pausing the live log view is the panel view's own action and the server keeps streaming to every subscribed client regardless; the response carries NO `status` field (distinguishing it from the `sendStreamResponse` shape a working action would use); the client's subscription is unaffected — a log line generated immediately after still reaches the panel (D-19: pause/resume are removed from the remote surface, not implemented, because the framework has no per-viewer identity to honour a per-viewer pause with — the pre-#434-fix behaviour and the #434 fix both silently kept delivering to a real client regardless of any pause request; this row now asserts that explicitly, rather than an unreachable positive claim) | server | |
| ultitools.remote.log-stream-levels | `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; a log-stream session already open (`ultitools.capability.logs`, appears after it) | Send `log_stream` `action: "config"` with `levels: ["error"]`; generate one generic INFO-level line and one generic ERROR-level line | The INFO-level line does NOT reach the panel's log stream while the ERROR-level line does — before this fix both would have reached it regardless of the requested `levels`, since the branch only logged that a request was received | server | |
| ultitools.remote.log-stream-batch-interval | `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; a log-stream session already open with batching enabled (`ultitools.capability.logs`, appears after it) | Send `log_stream` `action: "config"` with `batchConfig: {interval: 15000}`; generate log lines steadily; time the gap between two successive `logs` entries in the `batch_update` stream | The observed gap between successive batch sends is approximately 15 seconds, changed from the previous cadence WITHOUT restarting the server — before this fix, `setIntervalMs` only mutated a field the already-running scheduled task never re-read, so the cadence never changed no matter what was sent (#432, the runtime half; `ultitools.config.config-yml`'s step (e) separately proves the boot-time `config.yml` half) | server | |
| ultitools.remote.log-stream-batch-interval.neg-below-floor | Same as `ultitools.remote.log-stream-batch-interval` | Send `log_stream` `action: "config"` with `batchConfig: {interval: 1}` | Rejected: an error response naming the 1000ms floor; the previously-effective interval is unchanged, confirmed by the batch cadence continuing at its prior pace, not driven to roughly 1000 sends/second (WR-01: the panel's own live path used to accept any positive value, more permissive than the boot-time path's own 1000ms clamp) | server | |
| ultitools.remote.error-auto-report | `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session; a log-stream session already open with `levels` set to exclude `"error"` (e.g. `["info", "warning"]`) | Trigger a genuine `SEVERE`-with-exception log record (e.g. an uncaught exception from a command or event handler) while `"error"` is excluded from the live view's `levels` | The line does NOT appear in the live `log_stream`/`log_batch` view (the `levels` filter is working as `ultitools.remote.log-stream-levels` proves), but the error still appears in the panel's separate error-reporting surface (the `errors` array in the next `batch_update`, or via `ErrorReportCollector`'s own drain) — the two surfaces are independent (CR-02: making `levels` genuinely effective must not silently disable automatic error reporting as a side effect) | server | |
| ultitools.remote.log-stream-start-stop | Addendum (D-20, issue #468). `ultipanel.capabilities.logs: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; the panel already receiving live log lines (delivery is automatic once connected — no `start` step is needed or effective) | Send `log_stream` `action: "stop"`, then `action: "start"`, both for an arbitrary `clientId` | Both receive an error response naming the action ("start"/"stop") "not supported", stating this framework streams logs to the panel for as long as it is connected with no per-viewer on/off state, and that turning the live view on/off is the panel view's own action; neither response carries a `status` field (the `sendErrorResponse` shape, not `sendStreamResponse`'s); log delivery is completely unaffected throughout — a log line generated immediately after the `stop` request still reaches the panel (D-20: `start`/`stop` never actually gated delivery on any released version, before or after this change — this row asserts that explicitly, rather than an unreachable positive claim that `stop` silences the stream) | server | |
| ultitools.remote.log-stream-status | Addendum (D-20, issue #468). Same preconditions as `ultitools.remote.log-stream-start-stop` | Send `log_stream` `action: "status"` for an arbitrary `clientId` | The response's `data` carries `action: "status"`, a boolean `connected` field reflecting the plugin's live WebSocket connection state, `logTransmitterEnabled`, and `queueSize` — it does NOT carry `subscriberCount` or `streaming` (removed: this framework has no per-viewer subscription concept to count, and delivery is unconditional once connected, which is exactly what `connected` reports honestly instead) | server | |

## Panel upload and metrics honesty

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.remote.upload-config | `ultipanel.capabilities.file-write: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send an `upload_config` panel message with `configType: "plugin_config"`, a valid `configContent` JSON object, `configName`, `format`, `backup` | The config content is applied through `ConfigEditorUtils#updateConfigMap` and the response is `upload_config_response` with `status: "success"` | server | |
| ultitools.remote.upload-config.neg-permissions | Same as above; an authorized UltiCloud session under the maintainer's delegated authorization | Send an `upload_config` panel message with `configType: "permissions"` | Refused with an `error` response whose `data.message` is exactly `Failed to upload config: Unsupported config type: permissions` — before this fix the same request logged at a hidden `FINE` level and answered `success` for a request that did nothing (#435) | server | |
| ultitools.remote.disk-usage | `ultipanel.capabilities.monitoring: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; shell access to the same host to run `df` | Observe one `batch_update` cycle's (or send a `metrics_data` request and read its) `data.serverPerformance.diskUsage`; separately run `df` against the mount holding the server's working directory and compute its used-percentage | `diskUsage` is strictly greater than `0` and at most `100`, and is within one percentage point of the used-percentage `df` reports for the same mount — a range alone would pass even on a hardcoded nonzero constant, which is why the `df` comparison is required; before this fix the field was a hardcoded `0.0` regardless of the real filesystem (#436) | server | |
| ultitools.remote.enabled-plugins | `ultipanel.capabilities.monitoring: true` (shipped default); an authorized UltiCloud session under the maintainer's delegated authorization; a way to put one non-essential, already-loaded plugin into the loaded-but-disabled state without removing it from `Bukkit.getPluginManager().getPlugins()` (e.g. `Bukkit.getPluginManager().disablePlugin(target)` from a console script, or a plugin whose `onEnable()` throws due to a temporarily-broken soft dependency — restored once the row is done) | Send a `plugin_list` request (carrying a `requestId`) and record `data.totalCount` and `pluginUsage.enabledPlugins` from the next `metrics_data`/`batch_update`; put the chosen plugin into the loaded-but-disabled state; send another `plugin_list` request and record `metrics_data`'s `enabledPlugins` again | `plugin_list`'s `data.totalCount` (every installed plugin, the now-disabled one included with its own entry showing `enabled: false`) is unchanged between the two readings, but `pluginUsage.enabledPlugins` is exactly one lower than the recorded baseline — before this fix `enabledPlugins` always equalled the installed count regardless of how many were actually enabled (#437) | server | |

## Remote surface guards

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.remote.command-blocklist | `ultipanel.capabilities.commands: true`; blocklist at its shipped default; an authorized UltiCloud session under the maintainer's delegated authorization | Send an `execute_command` panel message for `stop` | `CommandExecutionManager#isCommandAllowed` refuses it; `RemoteActionLog` records a `DENIED` verdict citing `ultipanel.commands.blocklist` and naming `stop` | server | |
| ultitools.remote.command-blocklist.neg-namespaced | Same as above; an authorized UltiCloud session under the maintainer's delegated authorization | Send an `execute_command` panel message for `minecraft:stop` | Refused identically to the bare `stop` case — the namespace prefix is stripped before the blocklist check | server | |
| ultitools.remote.file-editable-roots | `ultipanel.capabilities.file-read: true`; editable roots at their shipped default (`plugins`, `logs`); the target is an EXISTING regular file inside `plugins/` with an allowed extension and not a protected name (e.g. a plain `.txt` file, not a `.jar`/`.class` or a credential-bearing filename) — `FileOperationManager#deniedUnconditionally` refuses those even inside an editable root, and a nonexistent path returns `File not found` regardless of the root check; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-read panel message for that file; SEPARATELY, send a file-list panel message for `plugins/` itself — `handleListOperation` is a distinct switch branch from the read path, with its own response shape (`data.files` array, `data.totalCount`), so a read-only test cannot prove listing still works | The read succeeds; the list request returns `file_operation_result` (`operation: "list"`) with `data.files` containing the target file's own entry and `data.totalCount` matching the array length | server | |
| ultitools.remote.file-editable-roots.neg-outside-root | `ultipanel.capabilities.file-read: true`; editable roots at their shipped default (`plugins`, `logs`); an existing regular file outside every configured editable root (e.g. a plain `.txt` file under the server's own `world/` folder); an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-read panel message for that file | Refused, naming the path as outside the editable-root set | server | |
| ultitools.remote.file-editable-roots.neg-protected | `ultipanel.capabilities.file-read: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a file-read panel message for `server.properties` | Refused unconditionally — `'server.properties' is a protected server file` — regardless of the editable-root and capability settings | server | |
| ultitools.remote.server-properties-safe-keys-read | `ultipanel.capabilities.server-properties: true`; an intact, unmodified `server.properties` — every one of the fourteen `SAFE_KEYS` entries is a standard Bukkit/Paper default key, so a normally-started Paper server (not hand-stripped of any of these fourteen lines) satisfies this — `ServerPropertiesManager#getSafeProperties()` adds a key only when `Properties#getProperty` returns non-null, so a genuinely missing or unreadable key would be silently absent from the response, not a fail on its own; an authorized UltiCloud session under the maintainer's delegated authorization — this row is dispatched through the same real panel WebSocket route as its 23 `capability.*`/`remote.*` siblings and is blocked by the identical shared-server-activation gate (D-03a), so it carries the same marking they do rather than the unmarked `server` Layer it previously carried inconsistently | Send a `server_properties` message with `action: "get"` (or omit `action`, which defaults to `get`) | `ServerPropertiesManager#handleGet` returns a `server_properties_result` whose `data` is a flat `{key: value}` map covering exactly the `SAFE_KEYS` entries actually present in `server.properties` — all fourteen (`motd`, `max-players`, `view-distance`, `simulation-distance`, `spawn-protection`, `difficulty`, `gamemode`, `pvp`, `allow-nether`, `allow-flight`, `spawn-animals`, `spawn-monsters`, `spawn-npcs`, `enable-command-block`) given the precondition above; no key outside that set (e.g. `online-mode`) is present, even though it exists in the real `server.properties` file | server | |
| ultitools.remote.server-properties-safe-keys | `ultipanel.capabilities.server-properties: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a `server_properties` edit for `motd` | The key is written | server | |
| ultitools.remote.server-properties-safe-keys.neg-unsafe-key | `ultipanel.capabilities.server-properties: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Send a `server_properties` `action: "set"` edit for a key not on the safe list (e.g. `online-mode`) | The response reports `success: false` with `reason: "Key is not in the allowed list"`, and `server.properties` is unchanged | server | |
| ultitools.remote.server-properties-safe-keys.neg-absent-on-server | `ultipanel.capabilities.server-properties: true`; a `SAFE_KEYS` entry that THIS server's own `server.properties` genuinely does not have (verify with `grep` against the real file before dispatching — SAFE_KEYS is a ceiling across every Paper version this framework supports, not a per-server promise; a key present with an empty value still counts as present); an authorized UltiCloud session under the maintainer's delegated authorization | Record the file's checksum (`sha256sum server.properties`); send a `server_properties` `action: "set"` edit for that key | The response reports `success: false` with `reason: "This server version has no such key"`, distinguishable from the `.neg-unsafe-key` case's rejection reason; the file's checksum recorded afterward is unchanged (D-15, SAFE_KEYS issue) | server | |

## Configuration

One row per shipped yml file (D-06's config-per-file rule), not per key: `config.yml` (44 keys)
and `env.yml` (2 operator-relevant keys, `api-url` and `version`). Each row confirms every key in the file is present at
its `FEATURES.md`-documented default, then flips one representative key and observes the
behaviour follow.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitools.config.config-yml | Fresh `plugins/UltiTools/config.yml` at its shipped default (not hand-edited); server started at least once, so the two migrated keys have run | Load the file; confirm each of the 47 keys listed under this document's companion `FEATURES.md` `## Configuration` section is present at its documented default, PLUS the two migrated keys (`ultipanel.commands.blocklist`, `ultipanel.files.editable-roots`) present at their migrated default; then set `language: "en"`, restart the server, and run `/ul help`; separately, exercise the five keys below with values that DIFFER from `SystemLogHandler`/`UltiPanelLogTransmitter`'s own in-code defaults, in separate configurations so no filter masks another's effect (copying `config-example.yml` verbatim proves nothing — its shipped values happen to equal the in-code defaults, and Bukkit accepts an unrecognized key silently, so a misspelled or ignored key would look identical; `ultipanel.logging.levels`/`excluded-loggers` remain purely opt-in, absent from the shipped resource — the three `batch.*` keys are shipped as of this row's own key count above, but their effect is exercised here the same way): (a) set `ultipanel.logging.levels: ["error"]` only (`excluded-loggers` left at its default), restart, open a live panel log-stream session, and generate one generic INFO-level line and one generic ERROR-level line; (b) separately, set `ultipanel.logging.levels` back to a default-equivalent set that includes `info`, then append `Minecraft` to `ultipanel.logging.excluded-loggers` on top of its shipped default, restart, and open a live panel log-stream session; only after that session is established, trigger the observable — disable then re-enable the plain Bukkit plugin `UltiToolsExtExample` (`UltiTools-External-Example`, installed alongside a plugin manager able to disable/enable it without a restart, e.g. `Bukkit.getPluginManager().disablePlugin(target)`/`enablePlugin(target)` from a console script) — which makes `UltiToolsAPI#disconnect`/`#connect` log `[UltiTools-API] External plugin disconnected: UltiToolsExtExample` then `[UltiTools-API] External plugin registered: UltiToolsExtExample v<version>`, both via `Bukkit.getLogger()`, i.e. `Logger.getLogger("Minecraft")`, which is why `Minecraft` (not `UltiTools` or any class name) is the exclusion target — a boot-time line such as the plugin-load-order message cannot be used here instead: `LogStreamManager#initialize`, which attaches the capture, does not run until after that line has already been logged during boot, so a session opened afterward can never observe it (issue #487); confirm neither triggered message reaches the stream while unrelated stream traffic still arrives (the observable is this message text, never the panel payload's own `logger` field: that field is synthesised by `UltiPanelLogTransmitter#determineLoggerName` from the log's source bucket — `plugin:X` becomes `X`, the `server` bucket becomes the literal string `MinecraftServer`, anything else becomes the source string as-is — it is never the record's actual JUL logger name, so it cannot be used to observe this key's effect); then remove `Minecraft` from `excluded-loggers` again (back to the shipped default, which does not name it), restart, open a live panel log-stream session again, and repeat the disable/re-enable trigger, confirming both messages now reach the stream — testing the logger-exclusion key on its own, not combined with a `levels: ["error"]` filter that would suppress an INFO-level line regardless of the exclusion list; the shipped default entries (`com.mojang.authlib`, `net.minecraft.network`, `org.apache.http`, `com.zaxxer.hikari`, `org.eclipse.jetty`) are NOT usable for this check on Paper — they name loggers that log through Log4j and never become `java.util.logging` records, so `SystemLogHandler`, attached only to the JUL root logger, never sees them regardless of this key's value; (c) set `ultipanel.logging.batch.enabled: false`, restart, and generate one log line while watching for immediate delivery rather than a queued batch; (d) set `ultipanel.logging.batch.size: 2` (default 10) at the shipped default `ultipanel.capabilities.monitoring: true` (do NOT set `monitoring: false` for this key: that would leave `externalDrainMode` off and exercise only the older, always-worked `sendBatch()` path, not the production-representative one this step targets) — `LogStreamManager#initialize` puts the transmitter into `externalDrainMode` whenever monitoring is active, under which `UltiPanelLogTransmitter#addToBatch`'s size-threshold check no longer sends via `sendBatch()` directly (that path now early-returns under external drain mode) but instead invokes `ServerMonitorManager#drainLogsNow()` — an immediate, non-interval-gated drain distinct from the fixed 5-second `sendBatchUpdate()` cycle, and one that honours the transmitter's own configured `batchSize` rather than a hardcoded constant — restart, and generate exactly 2 log lines in quick succession, watching for an immediate flush rather than waiting for the next 5-second tick or the default queue depth of 10; (e) set `ultipanel.logging.batch.interval: 15000` (default 5000), restart, and time the gap between two successive `logs` entries in the panel's `batch_update` stream while sending log lines steadily (#432 fixed — this key's boot-time read now actually takes effect; `ultitools.remote.log-stream-batch-interval` above separately proves the panel's own live `config` action can change it again without a restart) | Every one of the 47 shipped-resource keys is present at its documented default before the change; both migrated keys are present in the on-disk file even though absent from the packaged resource; after the restart, `/ul help` prints the English `=== UltiTools Commands ===` block instead of the `lang/zh.json`-localized block, proving the flipped key took effect; with `levels: ["error"]`, the generated INFO-level line does NOT reach the panel's log stream while the generated ERROR-level line does; with `Minecraft` appended to `excluded-loggers` (and `info` still an enabled level), triggering the `UltiToolsExtExample` disable/re-enable after the log-stream session is already open produces neither `[UltiTools-API] External plugin disconnected: UltiToolsExtExample` nor `[UltiTools-API] External plugin registered: UltiToolsExtExample v<version>` in the panel's live log stream, while other stream traffic keeps arriving; with `Minecraft` removed again (shipped default), repeating the same trigger after the next restart produces both messages in the stream — proving the exclusion key suppresses a record by its actual JUL logger name (`Minecraft`, never the panel payload's own synthesised `logger` field) on its own, using a logger `SystemLogHandler` actually receives, unlike the shipped default entries, and using a trigger the tester controls after the capture is already attached rather than a boot-time line the capture cannot yet observe (issue #487); with `batch.enabled: false`, the generated line is delivered immediately, not held for a batch; with `batch.size: 2` (monitoring at its shipped default), the 2 queued lines flush together immediately via the size-triggered `drainLogsNow()` path, well before the next 5-second `sendBatchUpdate()` tick or the default 10-entry threshold would have fired; with `batch.interval: 15000`, the observed gap between successive batch sends is approximately 15 seconds, not the 5-second default the pre-fix code was stuck at | protocol | |
| ultitools.config.env-yml | The jar handed over for this dispatch — a single clean build of the current commit at its default Maven properties, no `-Dultitools.api.url` override — is a fresh, unmodified extraction target, not a hand-edited copy; the `pom.xml` `<version>` at the built commit known in advance (read it before extracting). This is a build-time (Maven-filtered) property: it is exercised by the build that produced the handed-over jar, not by editing the shipped `env.yml` file after the fact, and this row proves only that default-property build | Unzip the handed-over jar and read `env.yml` | `env.yml` reads `api-url: "https://api.ultikits.com"` and `version: <the pom.xml version>` (e.g. `6.3.0-SNAPSHOT`) — Maven's resource filtering resolved both placeholders, neither is the literal `${project.version}` token. This row proves only the default-property build; proving the `-Dultitools.api.url` override path requires a second, separately-built and separately-hashed jar handed over for that specific purpose — building one ad hoc during a dispatch is out of scope for this phase's boundary against constructing new product artifacts mid-run | protocol | |
