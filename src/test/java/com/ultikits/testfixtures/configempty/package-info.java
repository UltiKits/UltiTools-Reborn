/**
 * Deliberately empty - no {@code @ConfigEntity} class at all. {@code ConfigManager.registerAll}
 * scanning this package must return normally rather than throwing.
 * <p>
 * 故意留空——完全没有 {@code @ConfigEntity} 类。{@code ConfigManager.registerAll} 扫描这个包
 * 必须正常返回，而不是抛出异常。
 */
package com.ultikits.testfixtures.configempty;
