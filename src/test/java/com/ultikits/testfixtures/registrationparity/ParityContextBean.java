package com.ultikits.testfixtures.registrationparity;

/**
 * The {@code @ContextEntry} target for {@link ParityFixtureModule} -- a plain class with an
 * accessible no-arg constructor, reflectively instantiated and registered as a bean by the
 * shared assembly method (WIRE-06).
 * <br>
 * {@link ParityFixtureModule} 的 {@code @ContextEntry} 目标类——一个带有可访问无参构造器的普通
 * 类，由共享装配方法反射构造并注册为 bean（WIRE-06）。
 */
public class ParityContextBean {
}
