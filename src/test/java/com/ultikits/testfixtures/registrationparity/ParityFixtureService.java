package com.ultikits.testfixtures.registrationparity;

import com.ultikits.ultitools.annotations.Service;

/**
 * A trivial {@code @Service} bean, scanned from {@link ParityFixtureModule}'s own package.
 * <br>
 * 一个简单的 {@code @Service} bean，从 {@link ParityFixtureModule} 自己所在的包中被扫描到。
 */
@Service
public class ParityFixtureService {
}
