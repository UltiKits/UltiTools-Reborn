/**
 * Two independently-valid {@code @ConfigEntity} classes - scanning just this subpackage must
 * register both (#358 Part 1's "both classes valid" case).
 * <p>
 * 两个各自独立、校验通过的 {@code @ConfigEntity} 类——只扫描这个子包必须两个都注册成功
 * （#358 第一部分的「两个都合法」场景）。
 */
package com.ultikits.testfixtures.configstranded.ok;
